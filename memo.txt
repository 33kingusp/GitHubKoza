・そもそもGit(ギット)ってなんぞや
バージョン管理システム
差分

・ある程度の用語解説	
・Gitの用途
・使い方(概念とか)


Git(ギット)

Repository(リポジトリ)
→プロジェクトが入ったフォルダ

Commit(コミット)
→現在のバージョンを保存すること(差分を積む)
→積まれたバージョンそのもの

Branch(ブランチ)枝
→コミットを分岐させる
→ブランチする？

Merge(マージ)
→ブランチでの作業内容を合体させる

Stage(ステージ)
→次のコミットにファイルを登録する

試しに追記しました。

masterブランチだよ
developブランチを作ったよ

ローカルリポジトリ
リモートリポジトリ

・GitHub(ギットハブ)ってなんぞや
・（リモートリポジトリ）

・SourceTree(ソースツリー)をつかってみよう

PullRequest(プルリクエスト)
→マージしていいか聞くこと

Push(プッシュ)
→ローカルリポジトリの変更内容を、リモートリポジトリに適用すること
Pull(プル)
→リモートリポジトリの変更内容を、ローカルリポジトリに適用すること
→適用されるのは、今いるブランチだけ

Fetch(フェッチ)
→リモートリポジトリとローカルリポジトリの違いをみえるようにする
